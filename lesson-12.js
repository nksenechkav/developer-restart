const users = ["Oksana", "Anna", "John"]; 

users.forEach (function (user){
    console.log (`Hello, ${user}`);
});

const numbers = [1, 2, 3, 4, 5];
numbers.forEach (function (number) {
    console.log (`Number: ${number}`);  
});